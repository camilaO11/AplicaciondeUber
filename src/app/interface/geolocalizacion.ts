export interface Geolocalizacion {
id: number;
latitud_destino: { lat: number, lon: number };
latitud_origen: { lat: number, lon: number };
longitud_destino: { lat: number, lon: number };
longitud_origen: { lat: number, lon: number };
    
}
